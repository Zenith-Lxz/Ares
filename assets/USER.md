# 用户偏好

这个文件记录你的运维习惯与约定，会注入到 Agent 的上下文中。
可以随时编辑，改动即时生效。

## 约定

（示例，按需修改）

- 改配置文件前先备份，备份名加时间戳
- 优先用包管理器安装，不用 curl | sh
- 日志排查从 journalctl 开始，不要直接 grep 整个 /var/log

## 禁止

- 不要在未经我确认的情况下重启任何服务
- 不要修改防火墙规则