# 安全策略（Security Policy）

ARES 是一个能通过 SSH 操作生产服务器的工具 —— 它的风险等级比普通开发工具高一个数量级。本文档描述威胁模型、已知边界与漏洞报告流程。

## 支持范围

| 版本 | 支持 |
|---|---|
| M0+M1 及之后（代码落地后） | ✅ 安全修复 |
| 设计阶段（当前，仅文档） | ✅ 设计缺陷可报告，将在对应里程碑计划中修复 |

## 威胁模型（2026-08-05 定稿）

**信任边界**：审批交互前的人（确认/拒绝）是唯一决策点 —— 审批只呈现给真人（M1 终端 y/n、M3 页面按钮、M5a 起系统通知），Agent 的 `terminal_execute` 通道与审批 UI 物理隔离，无法替用户确认（2026-08-05 决策：移除 Touch ID，审批统一为确认/拒绝）。系统设计的核心不变量：**prompt injection 不能提权** —— 无论远程输出、档案、skill 如何被注入，Agent 最多只能**提议**危险操作，提议必然撞上策略判定与人工审批。

**已建模的威胁**：

1. **prompt injection**（远程命令输出 / 文件内容 / 档案 / skill / 记忆）
   - 防线：数据/指令分层（§11.3）、observer 全段匹配 + fail-closed（§14.1）、展示侧控制字符清洗、注入内容原样上报
2. **命令匹配绕过**（sudo/引号/变量/编码/选项重排）
   - 防线：规范化 + fail-closed；M2 升级 shell AST 判定基座
3. **恶意 MCP client 冒领身份**
   - 防线：进程级认证（签名校验 + TOFU）、Confirm 级不委托 client、会话预算
4. **凭据泄露**（同 uid 进程、进程列表、日志、LLM 上下文）
   - 防线：Keychain（登录钥匙串保护）、askpass fd 传递、redaction（含前缀型环境变量）、observer 不放 env
5. **审计伪造/重建**
   - 当前：tamper-evident（防篡改可发现，非防重建）—— **已知边界**
   - M5 加固：keyed hash（Keychain）+ 跨月成链 + HEAD 锚点 + syslog 外部锚点
6. **确认疲劳 / 误批**
   - 防线：审批横幅 sanitize + 固定行数（防注入伪造）、极高危操作追加输入完整主机名、每次变更操作都必须显式确认（无「记住此选择」）

**不在模型内**（有意）：操作系统自身妥协（攻击者已获 root）、LLM 供应商被攻陷、物理接触设备。

## 已知边界（如实声明）

- 审计链在 M5 前可被「能写审计文件的主体」重建 —— 见设计文档 §14.3。
- 命令策略在 M2 前基于字符串规范化（fail-closed 兜底），非完备 shell 解析。
- 未配置 LLM 时只有纯执行通道（`!` 前缀），Agent 能力不可用。
- 远程 sudo 需要密码的场景不支持（见设计文档 §7.2）—— 请配置 NOPASSWD 或人工执行。

## 数据与隐私

- 命令输出与文件内容会发送给你配置的第三方 LLM API；进入上下文前经脱敏，但脱敏不是加密。
- LLM API key 存 macOS Keychain，不落盘配置文件。
- 敏感主机建议使用本地模型（vLLM/Ollama）或关闭 Agent 通道。

## 报告漏洞

请 **不要** 公开披露（GitHub issue / 社交媒体）可能被利用的安全缺陷。

- 私有报告：仓库公开后使用 **GitHub Security Advisory 私有报告**（仓库主页 → Security → Report a vulnerability）—— 这是唯一通道，不暴露维护者邮箱
- 请包含：影响版本、复现步骤、影响分析、建议修复（如已分析）
- 响应承诺：7 天内确认，修复前不公开细节
- 公开前过渡期：仓库未公开时直接联系维护者本人（线下渠道）

## 安全相关代码的不变量（维护者必读）

1. `rm -rf` 语义分层：deny=根/顶级目录（不可覆盖）、confirm=任意递归删除（默认级别）、critical=极高危（确认之外追加输入完整主机名）—— 禁止重新引入 `rm -rf /*` 模式
2. observer 必须 `matches_all_segments`（全段匹配），任一段动态形态即 fail-closed
3. 审批不可由 Agent 代答：非交互 stdin 一律拒绝（fail-closed）；审批横幅必须过 `sanitize_for_display` 且行数固定（防注入伪造屏幕）
4. 一切进入终端/弹窗/通知的 LLM 生成文本必须过 `sanitize_for_display`
5. 拒绝/超时/禁止的操作同样写审计；校验收集全部断点
6. 新增命令绕过手法：先加红队测试，再修实现
